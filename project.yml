name: USMCCPaperPublicityHelper
options:
  bundleIdPrefix: org.usmcc
  deploymentTarget:
    macOS: "13.0"
targets:
  USMCCPaperPublicityHelper:
    type: application
    platform: macOS
    sources:
      - path: Sources/USMCCPaperPublicityHelper
    settings:
      base:
        SWIFT_VERSION: 5.9
        MACOSX_DEPLOYMENT_TARGET: "13.0"
        PRODUCT_BUNDLE_IDENTIFIER: org.usmcc.USMCCPaperPublicityHelper
        INFOPLIST_FILE: Sources/USMCCPaperPublicityHelper/Info.plist
schemes:
  USMCCPaperPublicityHelper:
    build:
      targets:
        USMCCPaperPublicityHelper: all
